const BASE_URL = "http://localhost:3000";

const AUTH_URL = `${BASE_URL}/auth`;
const PROFILE_URL = `${BASE_URL}/profile`;
const USER_URL = `${BASE_URL}/user`;

export { AUTH_URL, PROFILE_URL, USER_URL };
